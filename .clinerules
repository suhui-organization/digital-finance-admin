# =============================================================================
# Cline Rules — digital-finance-admin (管理后台)
# Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 + daisyUI
# =============================================================================

## 技术栈
- 框架: Next.js 16 (App Router)
- UI: React 19 + Tailwind CSS 4 + daisyUI 5
- 状态管理: React Context + useReducer
- 图标: @heroicons/react
- 二维码: qrcode
- 开发端口: 16010

## 目录结构约定
```
src/
  app/           # Next.js App Router 页面
  components/    # 可复用组件
  lib/           # 工具函数、API 客户端
  hooks/         # 自定义 Hooks
  types/         # TypeScript 类型定义
```

## 组件编写规范
- 使用函数组件 + Hooks，不使用 Class 组件
- 组件文件默认导出组件，命名导出类型
- 首屏组件使用服务端渲染，交互组件添加 "use client" 指令
- 所有组件 Props 必须定义 TypeScript 接口
- 使用 daisyUI 组件时，遵循其主题化方案

## Tailwind CSS 规范
- 优先使用 Tailwind 工具类，避免内联 style
- 自定义样式使用 @layer 指令
- 响应式断点: sm(640px) md(768px) lg(1024px) xl(1280px)

## API 调用规范
- 统一使用 fetch 封装，错误统一处理
- API base URL 通过环境变量 NEXT_PUBLIC_API_URL 配置
- 请求需携带 JWT token (Authorization: Bearer xxx)

## 测试
- 单元测试: Jest + React Testing Library
- E2E: Playwright

## Cline 行为规则
- 修改组件前，先检查是否影响其他页面
- 新增依赖前确认必要性
- 修改后运行 `yarn lint` 检查
- Tailwind 类名保持整洁，合理分组